import { JsonLdDirective } from './json-ld.directive';

describe('JsonLdDirective', () => {
  it('should create an instance', () => {
    const directive = new JsonLdDirective();
    expect(directive).toBeTruthy();
  });
});
